"""Song et al.'s score-based SDE models as a GMMD teacher.  ``_vendor/`` is the official PyTorch
implementation; :mod:`.configs` carries the official model/SDE configs as plain namespaces;
:mod:`.teacher` wraps checkpoint loading and the fixed-x_t transition sampler."""
